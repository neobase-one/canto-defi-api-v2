{
  "name": "api-gateway",
  "version": "0.0.1",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "build": "rimraf ./build",
    "redo": "rimraf ./build && node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "rimraf ./build && node src/server.js"
  },
  "dependencies": {
    "@apollo/gateway": "^2.1.1",
    "apollo-server-express": "^3.10.2",
    "compression": "^1.7.4",
    "express": "^4.18.1",
    "graphql": "^16.6.0"
  }
}
